# NOTE: This configuration is for DEV/STAGING environment on Render

server:
  port: 8081

spring:
  application:
    name: ekart-user-auth-service

  config:
    import: optional:configserver:http://ekart-config-server.onrender.com

  datasource:
    url: jdbc:postgresql://dpg-d0kueeogjchc73el9a3g-a.oregon-postgres.render.com:5432/ekartdb_rz47
#    username: ekart
#    password: zATZzIfUryHlusKi5hbWjkez2ttFOWBO
    username: ${SPRING_DATASOURCE_USERNAME:ekart}
    password: ${SPRING_DATASOURCE_PASSWORD:zATZzIfUryHlusKi5hbWjkez2ttFOWBO}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://ekart-eureka-server.onrender.com/eureka
    register-with-eureka: true
    fetch-registry: true

#  instance:
#    prefer-ip-address: true

jwt:
  secretkey: abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ